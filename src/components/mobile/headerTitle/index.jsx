const HeaderTitleMobile = ({ HeaderTitle }) => {
  return (
    <header className="bg-white flex items-center justify-between px-5 py-4">
      <h2>{HeaderTitle}</h2>
    </header>
  );
};

export default HeaderTitleMobile;
